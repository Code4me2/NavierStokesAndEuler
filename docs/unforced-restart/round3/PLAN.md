# Round3 — one selected periodic force, whole-cell curl and mean

## Authority, preservation, state

Work only in `/home/velvet/worktrees/unforced-restart-20260909T202951Z` and new designated research evidence. No original-checkout access, network publication, pushes, merges, baseline proof/config edits, or global dependency operations. User authorization supersedes historical no-commit rules **only for local preservation**.

Preserved source snapshot: `e970490cf8f0cfb41fca101c62702fab7347c17c`, ref `refs/heads/research/unforced-validated-snapshot-20260912T175850171580Z`; parent/current HEAD remains `597692fa5d55e07d810b2d96ead1a67972585425`. Temporary alternate index used with read-tree/write-tree/commit-tree and create-only update-ref; active index bytes unchanged. All 2,878 preexisting tracked/research/docs files preserved. Prior refs unchanged, including local `fork/main` at `26e896edbdbe1215c0d50ddba24b2b6453646f5f`; no remote query or write was made. This is local preservation, not publication.

See [receipt](snapshot/RECEIPT.json), [provenance](snapshot/PROVENANCE.md), [selected paths/hashes](snapshot/selected-paths.json), [excluded paths](snapshot/excluded-paths.json), and [historical link audit](snapshot/documentation-links.json). Snapshot adds 64 explicit source/documentation/provenance paths; baseline tree inherited unchanged. Mathematical acceptance is exactly 14 sources + two audits, byte-identical to clean replay. Setup probe and scripts are tooling, not accepted mathematics. Giant logs, objects, caches, build symlinks, failed drafts and unrelated untracked files are not staged. Historical documentation references to excluded evidence are documented, not rewritten.

Read round-one FINAL-REPORT, Round2 REPORT, VALIDATION-LEDGER and CLEAN-RECOVERY together: the first three retain historical limitations; recovery records `SOURCE_BUILT_RESEARCH_CHECKS_PASS`, 16/16 strict checks, 96 exports and 42,939 project/research closures with only propext/Classical.choice/Quot.sound. Snapshot creation checked every entry of the external evidence hash manifest and all 16 source-copy hashes/exits; it did not rerun frozen validators or rewrite their path/HEAD/classification constraints. The snapshot itself was not the historical replay HEAD.

External evidence (frozen read-only input): `/home/velvet/research-builds/unforced-round2-clean-20260909T221339Z-evidence-attempt2/`. Its successful source build and research replay are complete; no duplicate build or no-cache approval gate. External dependency checkout: `/home/velvet/research-builds/unforced-round2-clean-20260909T221339Z/`, READ-ONLY. Installed Lean 4.34.0-rc2/core/compiler/runtime and host OS/hardware are explicit trust roots, not bootstrapped. Hashes and durable git objects suffice; no WORM, sudo or new permission gate.

## Initial-pass handoff

The sequential initial investigation is complete at focused/source-analysis scope; see [REPORT](REPORT.md). `WitnessFeasibility.selected` now provides the single coherent record, with strict exit 0 and frozen source SHA-256 `7d77092a3da73faedebac0a052c6fd2ce6fc46d340f48f796dd5e45f926be8da`. Other roles supplied source/ordinary-analysis reports, not new Lean acceptance. In particular MeanTopology gives the symbolic compact-integration mean-zero argument; its Lean adapters remain outstanding. The annular curl and interval identity remain unresolved. Later planning language below is the contract, not a claim those obligations were discharged.

## Exclusive ownership

Assignments are ownership, not claims of concurrent agents or completed proofs. In this initial pass the assistant acts sequentially; later handoffs must name the receiving owner. No role edits another role's files.

| Role | Only owned source/output subtree | Only owned report | Responsibility |
|---|---|---|---|
| WitnessFeasibility | `Research/UnforcedRestart/Round3/WitnessFeasibility/` | `docs/unforced-restart/round3/WitnessFeasibility.md` | Fix one coherent selection; expose actual fields, parameters and jets; distinguish symbolic existence from effective evaluation. |
| CurlGeometry | `Research/UnforcedRestart/Round3/CurlGeometry/` | `docs/unforced-restart/round3/CurlGeometry.md` | Curl component/sign on the entire cell, especially annulus; derivatives must come from germs/jet identities, never differentiated point values. |
| MeanTopology | `Research/UnforcedRestart/Round3/MeanTopology/` | `docs/unforced-restart/round3/MeanTopology.md` | Actual mean and cycle obstructions; compact divergence/residual integration and terminal continuity; periodic potential contract. |
| LocalizedForce | `Research/UnforcedRestart/Round3/LocalizedForce/` | `docs/unforced-restart/round3/LocalizedForce.md` | Full localization residual, including cutoff derivatives, mixed direct angular field and separated periodization; certified evaluation/tail bounds if available. |

Coordinator owns only `Round3/Integration/`, this PLAN, final REPORT and `round3/snapshot/`. All earlier files remain frozen byte-for-byte. Each role has exclusive unique `out/<run>/` and scratch paths. No shared mutable output, no imports from moving worker outputs; cross-role dependencies require a written hash freeze. Reviewers are read-only.

## Exactly one witness and fixed parameters

Take `ActualCandidateAssembly.selected_witness` **once**, retaining its schedule, all three sums, away extensions and periodic forcing together. If a data-valued wrapper is introduced, choose one complete record once and project it, not independent existential witnesses for each role. Until that wrapper is checked/frozen, the common mathematical convention is a single `obtain` of this theorem held fixed throughout all arguments. Do not use an arbitrary CandidateProperties instance or numerically invented replacement.

Fixed parameters: viscosity ν=1, terminal time T=1, unit spatial periods, restart t0=1/2 and H=1/2 for any subsequent comparison. Every S<H restricts these same fields, datum and restart. Budget B=`selectedBudget`=0; N0=`selectedThreshold`=`ActualCarrierGeometry.startingThreshold 0`; use the actual geometry proof, not an invented numerical threshold. h is the actual primary selected exponent (0<h<1/2), not freely reset to 1/4. Keep the selected diagonal schedule a, all cutoff profiles and other upstream choices fixed.

Source map (paths below start `NavierStokes/`):

* `ActualCandidateConstruction.lean:110–115`: B, N0 and geometry proof.
* `CorrectionInitialization.lean`, namespace `CorrectionInitialization.ActualPrimary`: outgoing/certificate/modulation/nominal/h, upper, and `choice B N0` (Classical.choice at line 3010); prepared geometry/covariance/phase data. Unfold the aliases before claiming numeric data.
* `ActualCandidateAssembly.lean:1019–1074`: `Witness`, `witness`, `selected_witness`. Actual `potentialStages`, `directStages`, `pressureStages`; common SelectedSchedule. ASum, BSum, PSum are `SolenoidalDiagonal.potentialSum (fun j => (a j : ℝ)) (PhysicalWaveSum.physicalQ h)` applied to those respective sequences. `ea,eb,ep` certify AwayExtensions. The periodic force and compact force are distinct existential components: do not infer their equality merely from this record.
* `MixedPeriodicAssembly.lean`: `cutVelocity A v = curl(χ A)+χ v`, `periodicVelocity`, `cutResidual`, `representative`, `boundaryLimits` (345), `exists_candidate_force` (539). Actual periodic u is activated periodicVelocity ASum BSum; p is activated periodicPressure PSum. Use local separated-copy germ theorems to move derivatives, not nonlinear lattice superposition.
* `SpatialLocalization.lean:39–57,110–111`: χ(x)=cutoff(16(x₀²+x₁²)) cutoff(4x₂); support cylinder r²≤1/16, |x₂|≤1/4; plateau r²<1/32, |x₂|<1/8. Localization annulus means supportCylinder minus plateau, including radial transition, axial caps and boundaries. It is **not** just the correction iteration's active annulus.
* `TimeLocalization.lean`: activation and its eventual-one germ; retain all activation residual terms before that regime.
* `JointResidualLimits.lean:81–130`: AwayExtensions, VanishingJointJets, boundaryLimits (zero at origin; otherwise derivatives of a chosen smooth local extension). `MixedPeriodicAssembly.boundaryLimits` reads the cut residual at representative x.
* `CandidateFromLimits.lean:83–117`: smooth extension force, equality with activated residual for 0≤t<1, shutdown only for t≥2, boundary jets. `selected_witness` supplies its periodic force's full boundary-jet identity directly.

## Mathematical target and permissible conclusions

Let f be that fixed periodic force. Q=[0,1]³ (unit cube; boundary has measure zero). Evaluate/prove

`c*(x)=curl_x f(1,x)` for every x∈Q, and `m*=∫Q f(1,x) dx`.

Use spatial curl with components `(∂₁f₂−∂₂f₁, ∂₂f₀−∂₀f₂, ∂₀f₁−∂₁f₀)` (indices 0,1,2). Match repository curl conventions explicitly. The order-one full jet at (1,x), restricted to directions (0,e_i), determines these components; order zero determines the force. No reference velocity regularity at T is assumed. Smooth force supplies continuity of curl and cell mean once the integration adapter is justified.

Effective terminal force means the nongradient component: for smooth periodic f, a periodic gradient requires curl=0 **and** zero harmonic mean/cycle periods. The torus sufficiency/potential construction, time regularity and corrected pressure admissibility must be supplied, not smuggled in by notation P f. A nonzero constant force is curl-free but not absorbable by periodic pressure.

Cover the plateau, localization annulus/caps, support boundary/exterior, lattice translates and cell seams. Origin flatness only handles lattice copies of the origin. At nonzero representatives, express the actual cut-residual extension jet, including `∇χ×A`, χ BSum, pressure cutoff and every viscous/nonlinear commutator. A scalar base formula at the axis does not give these annular derivatives.

Numerical evaluation needs finite representations or certified approximants to **this** choice, with derivative/tail error bounds, exact parameter identification and enclosure of the claimed curl component/mean. Existential noncomputability supplies no numerical data. Conversely, noncomputable selection does not prevent symbolic proofs uniform over all allowed selections. Return the smallest missing identity/estimate if no evaluation certificate exists.

Terminal tests have precise scope:

* Nonzero c*(x*) or m*, together with the appropriate continuity, gives a neighborhood of T with a nonzero obstruction; hence rules out arbitrarily late exact pressure-absorption slabs.
* A nonzero value only at t1<T rules out only slabs containing t1. It does not rule out all later slabs.
* Zero at one point, zero curl/mean at T over the whole cell, or even f(T,·)=0 does **not** prove f(t,·)=∇φ(t,·) throughout a terminal interval.
* Literal zero f preserves both u and p. Absorbable nonzero gradient preserves u but changes pressure to **p−φ**, with spatial periodicity and adequate joint time regularity. These are distinct conclusions.

Final decision must choose: (1) exact removal with a full terminal-slab identity/admissible potential, or (2) a controlled perturbative route with actual projected-force bounds and fixed-restart growth-relative stability, or (3) a precisely unresolved obstruction. Mere membership/finite norms or a failed sufficient envelope cannot choose (2) or prove impossibility. No A/B claim without the full comparator, lifespan, admissibility and growth argument.

## Focused validation and handoff

Do not run frozen validators: they would reject new Lean paths or overwrite old outputs. New role checks use direct installed pinned Lean, `-j1 -DautoImplicit=false -DwarningAsError=true`, LEAN_NUM_THREADS=1, LEAN_SRC_PATH unset. LEAN_PATH starts with the unique new role output root, then only verified external roots in `snapshot/external-evidence.json`'s resolver. Existing external research replay objects may be read-only imported only at their verified frozen hashes. No old worktree .lake fallback, Lake elaboration, dependency fetch/update/install or source build.

Each check records exact command/env, source/output/log hashes and exit; scope source triage rejects sorry/admit/custom axioms/unsafe/native proof shortcuts/option suppression/challenge imports. Print axiom closures for every new substantive definition/theorem; acceptance whitelist is propext/Classical.choice/Quot.sound. A signature probe is not a new theorem. Integration must enumerate generated helpers and imported project closures before claiming aggregate Round3 acceptance; a focused compilation alone has narrower scope. Preserve failures in unique directories, not as accepted sources.

Recommended per-process bounds: one thread/CPU, 6 GiB, zero swap, 32 tasks, 600-second timeout; serialize calls in this sequential pass. Future workers use a coordinator scheduling handoff, not shared output locks or an unavailable human approval gate. Never raise caps silently. No giant inventory duplication or duplicate source build.

Validation commands for the preservation record (safe, no old outputs touched):

```sh
git rev-parse HEAD
git diff --exit-code 597692fa5d55e07d810b2d96ead1a67972585425 --
git diff --cached --exit-code
git show --stat e970490cf8f0cfb41fca101c62702fab7347c17c
git diff-tree --no-commit-id --name-only -r e970490cf8f0cfb41fca101c62702fab7347c17c
# Verify preserved hashes and snapshot source blobs using new Integration verification tooling.
```

Do not rerun `snapshot.py`: it is intentionally one-shot and refuses to overwrite its receipt. Role reports must return exact selected-field source map, classified results, full hypotheses/commands/exits/hashes, whole-cell coverage and remaining blockers. Current initial decision is **precisely unresolved**: no certified nonzero annular terminal curl, no selected whole-cell terminal mean proof, and no full terminal-slab gradient identity or controlled strong perturbation has yet been delivered.
