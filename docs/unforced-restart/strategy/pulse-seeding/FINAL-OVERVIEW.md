# Final verdict — bounded pulse seeding

**Accepted with qualifications as a discriminating finite-pulse diagnostic. No all-stage initial-data obstruction, autonomous construction, or positive evidence for unforced B.** Both independent reviews support this disposition; their concrete corrections are incorporated in [CALCULATION-FINAL.md](CALCULATION-FINAL.md).

## 1. What the representative pulse needs

Take the selected family's positive-sign first harmonic at a native carrier point with positive stress and active masks, in a sufficiently large band. This is a symbolic representative of the actual variable-coefficient construction, not an extracted numerical label or constant-shear substitute.

With `Q=2^(−ℓ)`, `L≈ℓ²`, its covariance-calibrated physical uncut entry amplitude is

`𝒜 exp(−G)`, where `𝒜≈Q^(−1/2−h/2)L^(1/4)√|T| × |masks|`.

Here `G=Λ−D>0` is the explicit integrated growth minus **actual leading viscosity**, proportional to `L`. The pulse starts from the prescribed nonzero modal seed `P(0)e₊`; shear amplifies it but does not create it from zero. This is an amplitude norm, not a lower bound at cosine zeros. The localized field itself vanishes near entry: its cutoff derivative supplies the principal activation residual, alongside other residuals.

The decisive actual-slot calculation gives propagator singular values `exp(G+O(1))` and `exp(−Λ−D+O(1))`. Realizing an arbitrary midpoint vector at the tiny seed budget allows only an `exp(−2Λ)` relative component in the second output singular direction. The designed pulse satisfies this condition automatically. Forward filtering can tolerate many initial directions; exponential backward selectivity is not universal fine-tuning of stress-producing seeds.

## 2. Can initial data or known nonlinear production supply it?

**Local preseeding is compatible with smoothness in size, but common-time preseeding is unproved.** The local seeds beat every fixed derivative loss, not necessarily analytic bounds. They occur at different times; at any fixed restart, sufficiently late constructed bands have zero support and hence zero localized trace.

Unchanged fine-carrier heat storage for a fixed wait `Δ` requires

`𝒜ℓ exp(Δ|ξℓ|²−Gℓ)`, with `|ξℓ|²≈Q^(−1−h)`.

This diverges along a uniformly controlled sequence if `max(0,−log 𝒜ℓ)=o(Q^(−1−h))`. Mere nonzero masks do not suffice. This rejects that conditional storage route, not earlier large-scale storage, later shearing, or general NS propagation. A smooth-Fourier obstruction additionally needs infinitely many distinct actual Fourier modes of one datum.

**Known nonlinear products do not independently originate the pulse.** Primary self-products have only `0,±2`; distinct constructed labels have separated supports. The same-label `2+(−1)=1` return has zero leading fast symbol. After cutoff and full curl repair its finite-stage response has upper scale `W(3/2−2κs)`, smaller than the primary's natural `W(1/2)` scale by `ε^(1−2κs)` up to polynomial factors. It already uses the primary: feedback, not an independent supplier. No favorable projected lower bound is established.

## 3. Meaning and next action

No general impossibility or force-necessity theorem follows. No unforced-B progress follows from algebra, library validation, or the heat surrogate.

**Stop this preload/force-deletion campaign on these inputs; launch nothing automatically.** No candidate autonomous mechanism is established. Reopening would require independently supplied parents or controlled earlier covector history, then exact obligations: integrated growing response and timing, both stress components, sustainable background, pressure/divergence and localization/tails, one common smooth periodic solenoidal datum, and closure of the full nonlinear equation with `f=0` along one evolution. Per-horizon restarts do not suffice. Lean work is not justified before those mathematical obligations are resolved.
