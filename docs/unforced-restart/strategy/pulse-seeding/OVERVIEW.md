# Provisional overview — one pulse’s seeding requirements

**Owned root:** `/home/velvet/pulse-seeding-analysis-nM8ujEl7`

## Strongest actual inference

The positive-sign primary pulse in paper Lemma 7.4 admits a quantitative **finite-slot** seed calculation, with its actual variable coefficients—not invented selected parameters. Its share of the target stress fixes its amplitude up to controlled constants. Writing `L≈ℓ²`, the required uncut entry seed is

`Q^(−1/2−h/2) L^(1/4) √|T| × masks × exp(−G)`,

where `G>0` is an explicit shear-minus-viscosity integral proportional to `L`. The localized field itself vanishes near entry; a cutoff derivative supplies its principal activation source. That source is not the complete external force.

The actual modal propagator has singular values comparable to `exp(G)` and `exp(−Λ−D)`, where `G=Λ−D`. Consequently, backward realization of an arbitrary midpoint vector at the tiny seed budget permits only an `exp(−2Λ)` relative component in the other output polarization. The designed pulse satisfies this restriction. This is a genuine quantitative discrimination, **not NS controllability**. Forward amplification can robustly select the growing direction; the calculation does not demand exponentially precise initial polarization for every stress-producing perturbation.

## Fixed restart and nonlinear supply

The favorable local seeds beat every fixed derivative loss, so their size alone does not obstruct smooth initial data. But they occur at different times. At any fixed restart, sufficiently late dyadic bands have no constructed physical support. Their nonzero uncut seeds cannot simply be read from those zero traces.

A specifically identified bad proposal is to store every future tiny-scale carrier unchanged while it diffuses: its backward heat cost is `exp(cΔ Q^(−1−h))`, overwhelming `exp(Cℓ²)`. This excludes that **frozen-carrier storage surrogate**, not earlier large-scale storage, subsequent shearing, nonlinear transfer, or the actual NS evolution. The source does not control those earlier histories.

Known nonlinear products do not furnish an independent supplier. A primary’s self-products have harmonics `0,±2`, not its fundamental. Same-label resonances also lose their leading fast-derivative contribution by tangency. Distinct constructed labels have disjoint supports. The first same-label return through a second harmonic is perturbatively smaller under the finite-stage modal bounds and already depends on the original seed. Other overlapping autonomous parents remain possible but unidentified; matching covectors alone would not establish polarization, timing, magnitude or pressure compatibility.

## Missing obligations and recommendation

Still missing: one common smooth periodic solenoidal datum, controlled earlier covector histories and seed timing, both stress components, compatible tails/localization, a sustainable background, and closure of the full nonlinear equation with `f=0` on one evolution. Finite-dimensional inverses and per-horizon restarts supply none of these. Zero original datum evolves zero unforced.

**Stop the all-stage preload/force-deletion campaign on the present inputs.** Further analysis needs an independently specified autonomous parent mechanism or controlled earlier wavevector history—not another algebra-only response identity or peripheral implementation. No positive unforced-B breakdown evidence is obtained. Historical timing/stability exclusions and both blocked Comparator gates remain unchanged; software regression/axiom checks are not kernel certification or B progress.

**Review:** [CALCULATION.md](CALCULATION.md) §3, equations (5)–(10); §4’s conditional storage test; §5’s interaction/order claims; §6’s literal producer and selection audit. [INPUTS.json](INPUTS.json) pins the exact inputs. No independent adversarial review is claimed.
