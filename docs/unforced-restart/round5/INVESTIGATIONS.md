# Read-only analytic assignments

Dispatch status: prepared, not launched by this session (no subagent tool available). All three owners must first read PLAN.md, the frozen Round3/4 reports and the literal Round3 selected bindings. Return analysis to the integrator; do not modify any repository file, receipts, refs, dependencies or build outputs. The integrator records reports only in Round5.

## A — terminal radial/exterior producer

**Question:** Can the actual anchored slow-base exterior yield unequal selected terminal Jacobian entries at some nonzero representative, hence arbitrarily-late nonzero curl by continuity?

Start from `ActualCandidateAssembly.{exteriorStages,initialPotential_stage,positive_stage}`, `TailGaugePotential`, `ModulatedExterior`, `SlowBaseEndpoint`, `MixedDiagonalExtensions`, `JointResidualLimits`, `MixedPeriodicAssembly.cutResidualExtension`, and Round3 CurlGeometry/Certificates.

1. Work first on the central plane z=0 with radial transition 1/32<r²<1/16; establish the exact physical-domain and inactive-annulus conditions near a nonzero point in the incoming past.
2. Determine which initial/correction/direct germs vanish, which actual base terms survive, and what the *selected* zeroth diagonal cutoff does. Retain the radial anchor at s=1 and all slow orders.
3. Derive the complete residual curl, not one localization term. If a cutoff ODE/compact-support argument forces existence of a nonzero component, give the full argument, endpoint conditions and nontriviality certificate for the actual base. Existential x is fine; a rational sample is unnecessary.
4. Bind the local extension to d.jets via extension uniqueness, then specify the continuity argument to target N. If reduction fails, identify the exact unavailable identity or possible cancellation; do not promote a heuristic sign.

Deliver: exact source declarations and domains, coordinate/sign calculation, complete analytic producer or minimal missing proposition, and Lean adapter/import estimate. Distinguish an actual existential analytic witness from an evaluable numerical one.

## B — caps, active annulus and schedule regimes

**Question:** Can the selected schedule force total curl nonzero arbitrarily late, or can it instead prove curl identically zero on a fixed final interval?

Start from `GermCandidateAssembly.exists_candidate_witness_of_finite_stages`, `MixedCandidateWitness.SelectedSchedule`, `MixedCandidateAssembly.StageEstimates.exists_schedule`, `MixedDiagonalSchedule`, `SolenoidalDiagonal`, `SmoothCutoffs`, `PhysicalWaveSum`, `ActualPolarCoverage`, `ActualCurrentWaveSupport`, `EndpointCoordinates`, `SpatialLocalization`, `ActualCandidateAssembly.stageRealizations/physicalData`.

1. Trace the schedule producer's actual guarantees, not a separately selected schedule. Record positivity/doubling, inverse-scale bounds, smooth sums and vanishing-jets scope. Check whether stronger properties really survive in the fixed Data contract or require unavailable equality of choices.
2. Map q(t,x) on plateau, caps and radial/cap overlap; determine when all stages vanish, stage zero remains, or multiple stages contribute. Prove needed regime existence from the actual bounds; do not invent h, a₀, qbig or retained index values.
3. At q>0 use existential finite-prefix germ equality if useful. Keep derivative cutoffs and nonlinear cross-stage interactions; finite prefixes alone do not give a sign or uniform terminal control.
4. Seek a family (t,x) with t approaching 1 and total curl nonzero, or all-region cancellation on a single τ<t<1. Account for plateau, interfaces, exterior and periodic copies. If only one finite-time witness is reachable, state its limited scope as W.

Deliver: schedule/source map, exact regional inequalities, strongest complete analytic route, and explicitly separated effective/numerical gaps. No numerical experiment is authorized by these symbolic assignments.

## C — global discrimination and independent cancellation audit

**Question:** Can a structural argument decide curl on terminal slabs without evaluating selected choice data?

Start from the exact candidate properties, Round4 selected compact residual/mean theorems, Round3 MeanTopology/SelectedBridge, `MixedPeriodicAssembly`, `TimeLocalization`, `SpatialCurl`, and available PDE uniqueness/energy/continuation sources located read-only.

1. Test contradiction from hypothetical zero curl on an arbitrary final slab. Explain exactly what pressure potential, compact or periodic domain, regularity, support and uniqueness/continuation theorem would be required. Do not infer regularity from general unforced 3D Navier–Stokes conjectures, or infer dynamical separation from localized force support.
2. Check whether a proven source-specific invariant, moment, circulation, or exterior identity gives a nonzero *total* curl pairing. A nonzero pairing needs integrability and actual evaluation; zero mean alone cannot decide curl. Initial zero data can obstruct absorption over the entire activation evolution but does not by itself obstruct every later slab.
3. Independently derive pressure cancellation and activation factors: with Ω=curl U, curl R(ρU,ρP)=ρ(∂tΩ−ΔΩ)+ρ′Ω+ρ² curl((U·∇)U). Verify divergence-free reduction and actual smoothness domains before commutation. Audit spatial cutoff-potential and direct angular contributions.
4. Give a complete route to N or Z if available; otherwise provide a precise conditional theorem and explain why it is not the main producer. When A/B reports arrive, audit their domains and quantifiers independently before integration.

Deliver: source-backed analytic proof or bounded no-route finding, hypotheses not supplied by the selected record, pressure/topology obligations for Z, and a quantifier audit (terminal nonzero → N; isolated preterminal nonzero → W only; terminal zero ≠ Z).

## Shared return format

* Literal selected bindings and target (Z, N, W, or auxiliary only).
* Source declaration table with exact hypotheses actually used.
* Full analytic derivation, including total cancellations and region coverage.
* Established vs proposed vs missing steps; no silent lemma assumptions.
* Existential analytic certificate vs numerical certificate assessment.
* Smallest Lean implementation path, expected new imports, and strongest honest conclusion.

Integrator owns implementation and validation after choosing the strongest genuine producer. Assignment alone is not investigation completion or mathematical evidence.
