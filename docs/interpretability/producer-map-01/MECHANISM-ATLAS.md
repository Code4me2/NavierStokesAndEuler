# Mechanism atlas — two different proof strategies

**Schema:** `mechanism-atlas/v1`; stable nodes `NS-01…08`, `EU-01…08`. Machine companion: [MECHANISM-ATLAS.json](MECHANISM-ATLAS.json), with pinned source anchors, artifact hashes, typed edges and evidence scopes. Source root: `/home/velvet/Desktop/NavierStokesAndEuler`; pin/observed HEAD: `5fdcfe346d399f68f19a820526b59b5326f28939`. Historical maps are preserved, not overwritten.

## Evidence and reconciliation

- **I — inherited source inspection:** the supplied maps report inspected local bodies; this is not a new inspection of their complete closures.
- **S — current source inspection:** the finite Euler correction chain and the displayed NS residual/physicalData interfaces were read here. No kernel execution.
- **H — human calculation:** explicit algebra from stated inputs, not an independently reviewed full proof.
- **U — unexpanded analytic input:** a located supplier remains explanation debt, not a demonstrated formal gap.
- **X — stronger attribution unresolved:** an existence exhibit does not establish extra properties of a classically selected witness.
- Paper/upstream interpretations and prior review/build/byte-check reports retain their original evidence class. Neither citation-byte agreement nor a lexical declaration match proves mathematics.

The root `Euler/MAP.*` expands the nested `NavierStokes/Euler/MAP.*`, rather than defining a second Euler route. Its M1–M8 supply this atlas's EU numbering. Nested E5's sign-sensitive pressure/renewal distinction is retained inside EU-04; nested E7 becomes EU-06/07. NS N1–N8 become NS-01…08. Macro arrows mean **mathematical use**, not necessarily direct calls or import edges; actual binding anchors are separate in JSON.

Published SOURCE-MAP's historical baseline is `597692fa…`, chapters 05–06 use `26e896e…`, and chapter 07 distinguishes retained sources from upstream `f9e8bc5…` and paper bytes. These are not interchangeable source witnesses. Here every production anchor resolves at the full current pin; older review claims are not recertified.

## Strategy A — NS: improve a designed residual, then extend it as forcing

The construction starts from a concentrating background and engineered waves. Shear extraction and viscous damping are substantive physical mechanisms, not arbitrary prescribed growth. Nevertheless, correction index J improves **residual accuracy**, not chronological evolution of an autonomous IVP. A diagonal produces one singular presolution whose remaining residual extends to a smooth external force. It does not produce exact unforced NS stages.

| Node | Producer → output consumed next | Identity, quantitative mechanism and evidence ceiling |
|---|---|---|
| NS-01 Native cycle | `ActualCyclePreservation.next_runInvariant` → `state_runInvariant` | One profile, B,N0, context and initialized iterate; σ_J=1/5+J/10. Analytic/coherent/periodic invariants survive together. Profile/cone, primary covariance, signed/temporal/rank closure remain U. I |
| NS-02 Local particular solve | `ActualCurrentParticularBounds.actual_potential_coefficient_class` → `GluedStageEstimates.current_potential_bound` | Incoming actual residual and frequency coherence; inverse-carrier gain ≤√ε. Use controlPatch **or zero amplitude germ**, not global denominator nonvanishing. Particular and post-particular signed inputs differ. I/U; chapter-06 selected numerical arrays remain X. |
| NS-03 Same physical prefix | `ActualPhysicalPrefixFields.physicalFields_of_stages` → `ActualCandidateAssembly.physicalData` | Five-field identity certificate: smooth velocity, differentiable pressure, two chart germs, exterior identity. Prefix has J+1 summands including stage zero and the initialized direct field. No quantitative residual bound is a field. I/S |
| NS-04 Physical residual rate | `selected_residual_jet_bound` → `finite_residual_rates` in `ActualCycleResidualBounds` | Native source+mean+excluded errors → comparable band q≤Q_n<2q → closure transfer → Cartesian jets. Gain h(1/2+σ) minus a fixed derivative loss; for iterate J weaken to hJ/10−loss(m). Native extraction and pullback estimate remain U. I/S |
| NS-05 Same-sequence stage estimates | `GluedStageEstimates.actualStageEstimates` → `ActualCandidateAssembly.estimates` | The same A_j,D_j,P_j have raw positive-stage, finite-background and finite-residual bounds. Constants may depend on j,m; losses are fixed before j,J. `StageEstimates` assumes no infinite residual conclusion. I/S/U |
| NS-06 One diagonal | `MixedCandidateAssembly.StageEstimates.exists_schedule` → `GermCandidateAssembly.exists_candidate_witness_of_finite_stages` | Choose one doubling schedule for all three sequences; then ∀m,r choose a sufficiently long finite prefix. Cutoff budget m≤j+2; curl costs one derivative, Laplacian two. Flat residual jets, **not zero residual**. I/H from existing companion; producers U. |
| NS-07 Extend force, localize fields | `CandidateFromLimits.force` → candidate witness assembly | Actual residual derivative traces → smooth extension; anchored potential, shrinking support, axis germs and away models preserve localization. Only force extends through the singular time. Periodic/compact outputs share raw sums; separate selected forces are not asserted definitionally equal. I/U |
| NS-08 Forced exclusion | selected periodic/compact candidates → `ComparatorTheorem` / `ComparatorR3Theorem` | Zero datum, smooth force; u_ν(t)=νu(νt), p_ν=ν²p(νt), f_ν=ν²f(νt). Slab uniqueness and terminal speed divergence exclude the corresponding global competitor. Whole-space pressure/class bridge remains U. No force deletion. I |

Edges: NS-01→02→03→04→05→06→07→08, with NS-01→04 and NS-03→05 supplying independent invariant/identity inputs. These are not claims that the first item alone supplies every input of the next.

**Domain ledger.** With firstBand=N and residualBand=N+1, qbig=Q_N, the actual residual chart satisfies q<2Q_n≤2Q_(N+1)≤qbig. Removing the extra band would lose the smoothness neighborhood. Native smoothness is on U; bounds are on an open strip V; selected points lie in U∩closure(V). Angular branches agree modulo 2π through stored periodic representation, not equality of angles. Exterior germs handle the complementary region. None of this exports chapter-06 numerical arrays on an arbitrary selected preparation's full enlarged domain.

## Strategy B — Euler: exact renewable stages, common datum, stability contradiction

Each stage is an exact unforced Euler evolution after solving its genuine correction equation. A common hierarchy supports amplified strain with controlled pressure and geometry. The limit taken across stages is an **initial datum**, not a trajectory limit at activation times. A hypothetical regular reference cannot track initial data converging in H3 while the stages' sampled gradients escape.

| Node | Producer → output consumed next | Identity, quantitative mechanism and evidence ceiling |
|---|---|---|
| EU-01 Fix hierarchy and recurse | `PacketInductionScales.exists_scales` → `PacketInfiniteConstruction.packets` | One S before all n; forward insertion at zero, joined insertion only with positive history. Not ∀n∃new scales. Full numerical/renewal guard closure U. I |
| EU-02 Parametrix and budget | initialized normal/spatial bounds → `initializedAllOrderBudget` | Leading primary tangency cancels normal transport. Full normalized field is O(Cv), drift is 2(3Cv+Cn)/k. B0=2Cv, B1=12Cv(4R), residual=2e^(−0.7X log k) are independent of correction order. Profile recursion/metric producers U; explicit scalar substitution I/H. |
| EU-03 Exact finite solve, then coherence | `finite_exists` → `Budget.family` → compatibility/field tower | Input q+2, retained q+1, equation q. One inverse metric/radius serves every q. Finite viscosities converge by quantitative Cauchy stability; uniqueness later identifies different-order inviscid solves. These are two different operations. I/S; local reconstruction selected below. |
| EU-04 Same corrected child | `initialized_uniform_flow_and_shear` → `GeometryJoinedChoice` → joined successor | Internal Q supplies its flow, velocity and signed pressure. Cylinder-to-physical differentiation costs k: C/k+E kΔ≤k^(−1/4). ApproximationResidual separately certifies actual Euler residual. Upper pressure curvature uses δ·goodRatio+badRatio, not the absolute Hessian budget. Renewal/physical transfer U; selected Q exports errors, not canonical numerical formulas (X). I |
| EU-05 Common initial datum | exact joined initial identity → `initialDataLimit_Hm` | Tail starts P_(1+i), shifts J→J+1 and X→x₁; retain u₁(0). ∀fixed m, stage-independent majorants are summable, giving the same smooth compact solenoidal datum. No time-path limit of stages. Majorant production U. I |
| EU-06 Reference stability | `eventually_h3_bound_varying` → sampled no-escape | Fix reference U on [0,T] and its H4 constant before n. Eventually T_n≤T; H3 error ≤640ε_n exp(3C_U T). No uniform stage H4 bound and no assumption activation samples precede T*. Underlying product/pressure estimates U. I |
| EU-07 Finite positive lifespan | initial local theory + no packet horizon → `lifespan` | Same initialDatum; 0<T*≤inf T_n≤1. No equality with activation limit. Local/continuation producers U. I |
| EU-08 Delivered classes | canonical curl confinement → compact-curl broad upgrade → `Euler/Solution.lean` | For S<T*≤T_n, restriction direction reverses relative to EU-06. Hypothetical broad competitor is upgraded and restarted, not the singular solution. Both Euler endpoints use the same datum/lifespan; BKM is not the final broad-class contradiction. I/U |

Edges: EU-01→02→03→04→05→06→07→08; also EU-01→04, EU-02→04, EU-04→06/08 and EU-05/06→08. EU-03's internal finite-viscosity and all-order coherence steps must not be conflated with NS-06's diagonal.

**Quantifier/domain ledger.** Cylinder = R³×(R/LZ), L>0, four spatial word directions; physical fields live on R³. Weighted metric energy sums roots of six-derivative base blocks with ρ^j/(j!)², not a physical H^j norm. Fixed initialized k/truncation precede correction q; packet spike δ differs from correction target Δ. Physical x/ell scaling must be restored. Finite law is interior in time, paths and initial traces are on [0,T]. All-order coherence uses the q=6 L² path and uniqueness, not a diagonal across derivative orders. Constants for initial summability or finite-viscosity comparison may depend on the fixed derivative order.

Broad R3 competitor classes require actual timewise L² plus a single global energy bound; smoothness alone is insufficient. Euler's all-order Sobolev lifespan class is not merely the broad class restricted to [0,T*). Periodic NS additionally requires periodic scalar pressure. Euler's physical `force=∇p` is not the external NS force.

## Three high-impact boundaries; exactly one selected

1. **SELECTED: EU-03 finite inviscid compactness and equation passage.** Exact upstream viscous family and consumer are present. Current inspection finds a tractable global Cauchy/interpolation producer, not an unspecified spatial compactness problem. Work starts with the actual family and its uniform energy bounds; proving those bounds remains upstream debt. See [RECONSTRUCTION-CONTRACT.md](RECONSTRUCTION-CONTRACT.md).
2. **Deferred: EU-04 cylinder-to-physical error and renewable geometry.** Central RI-05/OBL-EUL-002–003 debt. The maps now explain lineage and frequency arithmetic but not the full point-evaluation/inverse-map power bounds. This remains meaningful work; selected-witness strengthening is neither needed nor authorized.
3. **Deferred: NS-01/04 incoming residual and physical fixed-loss transfer.** NC-D2/NC-D5 and OBL-NSC-004–006. Five identity fields do not produce the quantitative native estimates. A whole cycle would widen into signed/mean/rank analysis; a local pullback reconstruction is possible later, not a parallel task now.

## Delta beyond published SOURCE-MAP and chapters 05–07

**Inherited new mapping evidence:** NS's five identity-only fields and literal suppliers, extra residual band, periodic-branch identity and radial closure; Euler's literal order-independent scalar suppliers, tangency-driven drift cancellation, finite-family identity, same-Q pressure/flow and paid derivative loss. These narrow historical obligations but do not close their analytic or review gates. Chapter 05 already contains the modal/clock/source calculation; chapter 06 already contains conditional numerical recurrences; chapter 07 already explains shear extraction, covariance and viscous tails. None is claimed as newly derived here.

**New in this synthesis:** separate proof strategies and index/choice conventions; retain both maps' qualifications and the sharper curvature submechanism; identify the actual finite-viscosity route as whole-sequence Cauchy convergence in C_tL², derivative-word interpolation to C_tH^(q+1), retained-cutoff continuity, projected-source convergence in C_tH^q, viscosity-defect convergence only in C_tL², then an integral/injective-map H^q derivative upgrade. This corrects the earlier NEXT-TARGET's proposed spatial-tail/equicontinuity checklist: those are not extra hypotheses of this route. It also bounds the task below the earlier demand to reprove all viscous Gevrey existence.

Historical RI-04/05, INT-001/005, NC-D1–D5, OBL-NSC/NSA/EUL and PM qualifications remain versioned debt. No global textbook proof, dependency audit, external-prose equivalence, fresh kernel check, unforced NS route, code change or build is claimed. Only this external output directory is written; sources, old reports and refs remain read-only.
